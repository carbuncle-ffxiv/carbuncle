import type { BaseTranslation } from '../../i18n-types.js';

const en_commands = {
  help: {
    commandNotFound: 'I could not find that command, can you try again?',
  },
} satisfies BaseTranslation;

export default en_commands;
